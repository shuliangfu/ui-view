/**
 * 与 shared 共用：桌面端路径 re-export（form/Transfer.tsx）。
 * 与 {@link ../../mobile/form/Transfer.tsx} 一致，单一实现见 {@link ../../shared/form/Transfer.tsx}；
 * `@dreamer/ui-view` 主入口已由 shared 导出 `Transfer`，本文件供 `@dreamer/ui-view/form` 等按路径对齐及插件图引用。
 */
export * from "../../shared/form/Transfer.tsx";
