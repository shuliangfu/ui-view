/**
 * Shared UI types used by view/react/preact implementations.
 */

/** Common size variants for buttons, inputs, etc. */
export type SizeVariant = "xs" | "sm" | "md" | "lg";

/** Common color/semantic variants for components */
export type ColorVariant =
  | "default"
  | "primary"
  | "secondary"
  | "success"
  | "warning"
  | "danger"
  | "ghost";
